import React from 'react';
import PropTypes from 'prop-types';
import './Logo.css';

const Logo = () => (
  <div className="Logo">
    Logo Component
  </div>
);

Logo.propTypes = {};

Logo.defaultProps = {};

export default Logo;
