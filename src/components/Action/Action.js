import React from 'react';
import PropTypes from 'prop-types';
import './Action.css';

const Action = () => (
  <div className="Action">
    Action Component
  </div>
);

Action.propTypes = {};

Action.defaultProps = {};

export default Action;
