import React from 'react';
import PropTypes from 'prop-types';
import './Member.css';

const Member = () => (
  <div className="Member">
    Member Component
  </div>
);

Member.propTypes = {};

Member.defaultProps = {};

export default Member;
