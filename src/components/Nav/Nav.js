import React from 'react';
import PropTypes from 'prop-types';
import './Nav.css';

const Nav = () => (
  <div className="Nav">
    Nav Component
  </div>
);

Nav.propTypes = {};

Nav.defaultProps = {};

export default Nav;
