import React from 'react';
import PropTypes from 'prop-types';
import './Sites.css';

const Sites = () => (
  <div className="Sites">
    Sites Component
  </div>
);

Sites.propTypes = {};

Sites.defaultProps = {};

export default Sites;
